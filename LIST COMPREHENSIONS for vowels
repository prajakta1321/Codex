#List Comprehensions


#Given a word as input, output a list, containing only the letters of the word that are not vowels.
#The vowels are a, e, i, o, u.

# Sample Input
# awesome

# Sample Output
# ['w', 's', 'm']

#code:

word = input("enter a word containing vowels :")
vowels =  ['a','e','i','o','u']

a = [i for i in word if i not in vowels]
print(a)
